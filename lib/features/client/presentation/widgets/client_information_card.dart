
import 'package:flutter/material.dart';

class ClientInforCard extends StatelessWidget {
  const ClientInforCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
