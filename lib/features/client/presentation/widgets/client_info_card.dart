import 'package:flutter/material.dart';

class ClientInformationCard extends StatelessWidget {
  const ClientInformationCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
